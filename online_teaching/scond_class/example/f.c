#include<stdio.h>
#include<limits.h>

int main(){
    int windows = INT_MAX;
    int doors = INT_MIN;
    printf("%d %d\n", windows, doors);
    long long long_num = LLONG_MAX;
    long long long_num1 = LLONG_MIN;
    printf("%lld %lld\n", long_num, long_num1);
    short _num = SHRT_MAX;
    short num_short =SHRT_MIN;
    printf("%d %d\n", _num, num_short);
    float e;// 用在小数点6位以内，数值范围int中
    double f; // 小数点后很多位，精确运算

    int printf = 1;
}